# 问题进展记录

## 2026-03-14（第 1 轮）

- 问题现象：文件浏览页点击具体音频、以及播放控制区“顺序播放/随机播放”后，无法进入播放页面并播放对应音乐。
- 根因排查：
  - 当前工程仅有 `MainActivity -> TvBrowseFragment`，此前未实现独立播放页面跳转逻辑。
  - ExoPlayer 默认不支持 `smb://`，原实现直接把 SMB URI 交给播放器，导致实际播放链路不可用。
- 已完成修复：
  - 新增 `PlaybackActivity`（播放页），并在触发播放时自动跳转。
  - `PlaybackService` 会话入口改为播放页，通知点击可直达播放页。
  - 新增 `SmbDataSource`（基于 `jcifs-ng`）与 `PlaybackConfigStore`，让 Media3 可读取 SMB 音频流。
  - 播放触发链路新增异常兜底提示，避免点击后“无反馈”。

## 2026-03-14（第 2 轮）

- 问题现象：
  - 播放页与 `spec/design.md` 不一致，缺少歌曲元信息、进度条、歌词显示。
  - 从播放页返回文件页后，播放控制区缺少“回到当前播放”入口。
  - 重新点击当前歌曲会重置播放，不符合“继续当前进度”的预期。
- 根因排查：
  - 播放页只实现了最小控制按钮，未订阅时长进度与歌词时间轴。
  - 浏览页播放控制区没有基于 MediaController 当前状态渲染“回到当前播放”按钮。
  - 点击歌曲时总是 `setMediaItems(...)` 重建队列，导致同曲重进后从头开始。
- 已完成修复：
  - 播放页新增：歌曲/艺术家/专辑信息、`00:00/总时长`、水平进度条、歌词三行（当前行高亮）。
  - 播放页新增封面加载（优先使用已有元数据里的 `artworkUri`，无图回退默认图）。
  - 浏览页“播放控制”新增“回到当前播放”按钮，并根据会话状态动态显示与标题。
  - 播放触发逻辑新增续播分支：若点击的是当前正在播且队列未变，仅恢复播放并跳转，不重置进度。
  - 队列一致但点击不同曲目时，改为在现有队列内定位曲目，不重建队列。
- 本轮验证：
  - `assembleDevDebug` 通过。

## 2026-03-15（第 3 轮）

- 问题反馈：
  - 播放页提示“暂无歌词”，无法验证歌词自动换行。
  - 封面仍未显示，无法验证非 1:1 显示效果。
  - 进度条选中后希望按 `OK` 也能播放/暂停。
- 根因与修复：
  - 修复外置歌词路径拼接 bug：配置了共享名且有子目录时，`.lrc` 路径遗漏共享段，导致歌词查找失败。
  - 封面加载兜底增强：同目录封面改为基于 `SmbFile(parent, name)` 的兄弟文件探测，减少 SMB 路径拼接带来的失配。
  - 进度条交互增强：在 `SeekBar` 聚焦时支持 `OK/Enter` 切换播放/暂停；左右键维持分档快进快退。
- 本轮验证：
  - `assembleDevDebug` 通过。

## 2026-03-15（第 4 轮）

- 输入：用户提供 `sample/トラック01.mp3` 与 `sample/トラック01.lrc` 用于验证歌词/封面。
- 验证结论：
  - `LrcParser` 可正常解析该 `.lrc`，且包含长文本歌词行。
  - `jaudiotagger` 可从该 `.mp3` 读取到内嵌封面（`firstArtwork` 非空且有字节数据）。
  - 定向单测 `SampleMediaValidationTest` 通过，说明样本资源本身有效。

## 2026-03-15（第 5 轮）

- 用户复测结果：
  - 歌词显示恢复正常，换行正常。
  - 进度条 `OK` 交互正常。
  - 封面仍未显示（样本 `sample/トラック01.mp3`）。
- 本轮修复：
  - 调整内嵌封面解析临时文件策略：临时文件后缀改为沿用原音频扩展名（不再固定 `.tmp`），提升 `jaudiotagger` 识别成功率。
- 本轮验证：
  - `assembleDevDebug` 通过。

## 2026-03-15（第 6 轮）

- 用户新反馈：
  - 封面可显示但加载滞后明显（10s~30s）。
  - `Track9` 场景下，歌词偶发“加载中 -> 暂无歌词”。
- 本轮优化：
  - 封面加载顺序调整为“元数据封面URI -> 内嵌封面 -> 同目录封面文件”，优先命中内嵌封面，减少同目录探测超时带来的首屏延迟。
  - 歌词加载新增重试机制（最多 3 次，短退避），降低 SMB 短时波动导致的误判“暂无歌词”。
  - 当播放页在会话恢复后拿不到内存态 SMB 配置时，新增 DataStore 回补配置，恢复歌词/封面读取能力。
  - 新增 `Track9` 样本验证用例，持续校验 `lrc` 解析与内嵌封面读取。
- 本轮验证：
  - `testDevDebugUnitTest --tests "*SampleMediaValidationTest"` 通过。
  - `assembleDevDebug` 通过。

## 2026-03-15（第 7 轮）

- 用户反馈：
  - `Track9` 仍存在“歌词加载中后显示暂无歌词”。
  - 封面已可显示但仍偏慢；返回文件页再回到播放页会重复加载封面。
- 本轮方案（先测后改）：
  - 新增 `SmbPathResolverTest`，先验证 share + 子目录 + `streamUri` 回退场景下外置 `.lrc` 路径构造正确。
  - 抽离 `SmbPathResolver` 到公共层，歌词仓库与播放页兜底读取统一使用同一套路径算法。
  - 播放页歌词新增进程内缓存 `PlaybackLyricsCache`，并在仓库失败时增加“基于 streamUri 的外置 LRC 直接读取”兜底。
  - 播放页封面新增进程内缓存 `PlaybackArtworkCache`，返回再进时优先命中缓存，避免重复 SMB 读取。
- 本轮验证：
  - `testDevDebugUnitTest --tests "*SmbPathResolverTest" --tests "*SampleMediaValidationTest"` 通过。
  - `assembleDevDebug` 通过。

## 2026-03-15（第 8 轮）

- 用户反馈：
  - 封面缓存已生效。
  - `Track9` 仍存在歌词误判“暂无歌词”。
  - 希望“回到当前播放”歌词也能命中缓存。
- 本轮修复：
  - 外置歌词查找改为多候选：优先 `streamUri` 同名替换 `.lrc`，再回退路径解析结果，覆盖更多 NAS 路径形态。
  - 文本解码增加 UTF-16 BOM 识别，兼容部分歌词文件编码。
  - 播放页歌词/封面缓存 key 统一改为 `uri` 优先，提升跨页面回到当前播放时命中率。
  - 补充 `Track9` 时间轴解析单测，确保样本可解析。
- 本轮验证：
  - `testDevDebugUnitTest --tests "*SmbPathResolverTest" --tests "*SampleMediaValidationTest"` 通过。
  - `assembleDevDebug` 通过。
