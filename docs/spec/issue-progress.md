# 问题进展记录

## 2026-03-14（第 1 轮）
- 修复点击音频/播放按钮无法进入播放页的问题。
- 新增 `PlaybackActivity`、`SmbDataSource`，打通 `smb://` 播放链路。

## 2026-03-14（第 2 轮）
- 播放页补齐歌曲元信息、进度条、歌词区域。
- 浏览页新增“回到当前播放”入口。
- 修复同曲重复点击导致重置进度问题。

## 2026-03-15（第 3 轮）
- 进度条支持 `OK/Enter` 播放暂停，左右键分档快进快退。
- 修复外置歌词 share+子目录路径拼接错误。

## 2026-03-15（第 4 轮）
- 使用样本 `sample/トラック01` 验证资源有效：
  - `.lrc` 可解析。
  - `.mp3` 可读内嵌封面。

## 2026-03-15（第 5 轮）
- 内嵌封面解析临时文件后缀改为沿用原音频扩展名，提升兼容性。

## 2026-03-15（第 6 轮）
- 封面加载顺序优化为：元数据 URI -> 内嵌封面 -> 同目录封面。
- 歌词读取新增重试，并增加会话恢复时的配置回补。

## 2026-03-15（第 7 轮）
- 抽离 `SmbPathResolver` 并新增单测。
- 新增 `PlaybackArtworkCache`、`PlaybackLyricsCache`，减少重复加载。

## 2026-03-15（第 8 轮）
- `Track9` 场景再优化：
  - 外置歌词改为多候选路径（`streamUri` 同名优先 + 回退路径）。
  - 增加 UTF-16 BOM 解码支持。
  - 缓存 key 统一为 `uri` 优先，提升回到当前播放命中率。
- 验证：
  - `testDevDebugUnitTest --tests "*SmbPathResolverTest" --tests "*SampleMediaValidationTest"` 通过。
  - `assembleDevDebug` 通过。

## 2026-03-15（当前状态）
- 用户复测结论：
  - 封面缓存已生效。
  - `Track9` 仍偶发“歌词加载中 -> 暂无歌词”（未闭环）。
- 后续优化方向（待下一轮）：
  - 为 `Track9` 增加端到端歌词加载日志与路径命中日志（debug）。
  - 增加歌词读取耗时与失败原因埋点，定位具体失败分支。
